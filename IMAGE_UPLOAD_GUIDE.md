# 📸 دليل رفع الصور - Image Upload Guide

## 🎯 نظرة عامة

تم تحديث التطبيق ليدعم **رفع الصور مباشرة من الجهاز** بدلاً من إدخال روابط URL فقط.

---

## ✨ الميزات الجديدة

### 1. رفع الصور من الجهاز
- رفع مباشر للصور من جهاز المستخدم
- معاينة فورية للصورة قبل الحفظ
- شريط تحميل يوضح حالة الرفع
- التحقق التلقائي من نوع وحجم الملف

### 2. إدخال رابط URL
- إمكانية إدخال رابط صورة خارجي
- معاينة الصورة قبل الحفظ
- التحقق من صحة الرابط

### 3. التبديل بين الوضعين
- زر للتبديل بين "رفع من الجهاز" و "إدخال رابط"
- مرونة كاملة للمستخدم

---

## 🔧 الأماكن المتأثرة

### 1. صورة العنصر (في قسم إدارة القوائم)
- **الموقع:** لوحة التحكم → إدارة القوائم → إضافة عنصر
- **الاستخدام:** رفع صورة للعنصر في القائمة (مثل صورة طبق طعام)

### 2. شعار الأدمن (في إعدادات الحساب)
- **الموقع:** لوحة التحكم → إعدادات الحساب → رابط الشعار
- **الاستخدام:** رفع شعار المطعم/الأدمن

### 3. خلفية الأدمن (في إعدادات الحساب)
- **الموقع:** لوحة التحكم → إعدادات الحساب → رابط الخلفية
- **الاستخدام:** رفع صورة خلفية للقائمة العامة

---

## 📋 القيود والشروط

### حجم الملف
- **الحد الأقصى:** 5 ميجابايت (5MB)
- **السبب:** للحفاظ على سرعة التحميل وتقليل استهلاك مساحة التخزين

### أنواع الملفات المسموح بها
- ✅ JPEG / JPG
- ✅ PNG
- ✅ GIF
- ✅ WebP

### التخزين
- **المنصة:** Vercel Blob Storage
- **المساحة المجانية:** 500MB شهرياً
- **السرعة:** عالية (CDN عالمي)
- **الأمان:** روابط آمنة ومستقرة

---

## 🚀 كيفية الاستخدام

### رفع صورة من الجهاز

1. اضغط على زر **"رفع من الجهاز"**
2. اضغط على **"اختيار ملف"**
3. اختر الصورة من جهازك
4. ستظهر معاينة فورية للصورة
5. سيتم رفع الصورة تلقائياً
6. احفظ النموذج

### إدخال رابط صورة

1. اضغط على زر **"إدخال رابط"**
2. ألصق رابط الصورة في الحقل
3. اضغط على **"تطبيق"**
4. ستظهر معاينة للصورة
5. احفظ النموذج

### إزالة صورة

- اضغط على أيقونة ❌ في أعلى يمين الصورة المعاينة

---

## 🛠️ الملفات المضافة

### 1. API Endpoint للرفع
**الملف:** `app/api/upload/route.ts`

**الوظيفة:**
- استقبال الصورة من المستخدم
- التحقق من نوع وحجم الملف
- رفع الصورة إلى Vercel Blob
- إرجاع رابط الصورة

**المسار:** `POST /api/upload`

**Headers المطلوبة:**
```
Authorization: Bearer {session_token}
Content-Type: multipart/form-data
```

**Body:**
```
file: (binary file data)
```

**Response (نجاح):**
```json
{
  "url": "https://blob.vercel-storage.com/...",
  "filename": "menu-images/1234567890-abc123.jpg",
  "size": 245678,
  "type": "image/jpeg"
}
```

**Response (خطأ):**
```json
{
  "error": "وصف الخطأ"
}
```

---

### 2. مكون ImageUploader
**الملف:** `components/ImageUploader.tsx`

**الوظيفة:**
- مكون React قابل لإعادة الاستخدام
- معاينة الصور
- رفع الصور
- التبديل بين الوضعين
- معالجة الأخطاء

**Props:**
```typescript
interface ImageUploaderProps {
  currentImageUrl?: string;      // رابط الصورة الحالية
  onImageUploaded: (url: string) => void;  // دالة تُستدعى عند رفع الصورة
  label: string;                 // عنوان الحقل
  helperText?: string;          // نص مساعد
}
```

**الاستخدام:**
```tsx
<ImageUploader
  currentImageUrl={imageUrl}
  onImageUploaded={(url) => setImageUrl(url)}
  label="صورة العنصر"
  helperText="يمكنك رفع صورة من جهازك أو إدخال رابط"
/>
```

---

### 3. تعديلات صفحة الإدمن
**الملف:** `app/admin/page.tsx`

**التعديلات:**
- استيراد مكون `ImageUploader`
- استبدال حقول `<input type="url">` بمكون `ImageUploader`
- في 3 أماكن:
  1. صورة العنصر (السطر ~557)
  2. شعار الأدمن (السطر ~702)
  3. خلفية الأدمن (السطر ~709)

---

## 🔐 الأمان

### المصادقة
- يتطلب الـ endpoint وجود session token صالح
- يتم التحقق من الـ token في كل طلب رفع

### التحقق من الملفات
- فحص نوع الملف (MIME type)
- فحص حجم الملف
- منع رفع ملفات ضارة

### التخزين الآمن
- الصور تُخزن في Vercel Blob (منصة آمنة)
- روابط دائمة ومستقرة
- CDN عالمي لسرعة الوصول

---

## 🐛 معالجة الأخطاء

### أخطاء شائعة

**1. "غير مصرح - يجب تسجيل الدخول"**
- **السبب:** Session token منتهي أو غير صحيح
- **الحل:** تسجيل الدخول مرة أخرى

**2. "حجم الملف كبير جداً"**
- **السبب:** حجم الصورة أكبر من 5MB
- **الحل:** ضغط الصورة أو اختيار صورة أصغر

**3. "نوع الملف غير مسموح"**
- **السبب:** الملف ليس صورة
- **الحل:** اختيار ملف صورة (JPG, PNG, GIF, WebP)

**4. "فشل رفع الصورة"**
- **السبب:** خطأ في الاتصال أو الخادم
- **الحل:** المحاولة مرة أخرى

---

## 📊 الأداء

### حجم البناء
```
Route: /api/upload
Size: 146 B
First Load JS: 102 kB
```

### وقت الرفع
- صورة 500KB: ~1-2 ثانية
- صورة 2MB: ~3-4 ثواني
- صورة 5MB: ~5-7 ثواني

*(يعتمد على سرعة الإنترنت)*

---

## 🎨 تحسينات مستقبلية (اختياري)

1. **ضغط الصور تلقائياً:**
   - تقليل حجم الصور قبل الرفع
   - تحسين الأداء

2. **السحب والإفلات (Drag & Drop):**
   - سحب الصورة وإفلاتها مباشرة
   - تجربة مستخدم أفضل

3. **قص الصور (Cropping):**
   - تحرير الصورة قبل الرفع
   - ضبط الحجم والنسبة

4. **معرض الصور:**
   - حفظ الصور المرفوعة سابقاً
   - إعادة استخدام الصور

---

## 💰 التكلفة

### Vercel Blob Pricing

**الخطة المجانية (Hobby):**
- 500MB مساحة شهرياً
- كافٍ لـ ~500-1000 صورة (حسب الحجم)
- بعد النفاد: الصور الجديدة لن تُرفع

**الخطة المدفوعة (Pro):**
- $0.15 لكل GB شهرياً
- نطاق ترددي غير محدود
- مناسبة للمشاريع الكبيرة

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من console للأخطاء
2. تحقق من network tab في DevTools
3. تحقق من صحة الـ BLOB_READ_WRITE_TOKEN في `.env.local`

---

## ✅ الخلاصة

تم تطبيق نظام رفع الصور بنجاح! الآن يمكن لأصحاب المطاعم:
- ✅ رفع صور المنتجات مباشرة من أجهزتهم
- ✅ رفع شعار المطعم
- ✅ رفع صورة خلفية مخصصة
- ✅ أو استخدام روابط URL كما كان سابقاً

**تجربة مستخدم أفضل وأسهل!** 🎉
