# تطبيق القوائم الذكية - دليل الاستخدام

## نظرة عامة
تطبيق Next.js لإنشاء وإدارة قوائم الطعام مع دعم مستخدمين متعددين، سمات قابلة للتخصيص، وتصميمات جذابة.

## المميزات
- 🎨 5 سمات ملونة مذهلة (المحيط، الغروب، الغابة، الملكي، الوردي)
- 👥 نظام مستخدمين متعدد مع حسابات منفصلة
- 🖼️ دعم الشعار والخلفية المخصصة
- 📱 تصميم متجاوب يعمل على جميع الأجهزة
- ⚡ سريع وسهل الاستخدام
- 🔐 نظام تسجيل دخول آمن

## كيفية الاستخدام

### 1. إنشاء حساب جديد (للأدمن الأول)
يمكنك إنشاء حساب أدمن جديد من خلال:
```bash
# استخدم API مباشرة لإنشاء أول أدمن
curl -X POST http://localhost:3000/api/admins \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "password123",
    "theme": "ocean"
  }'
```

### 2. تسجيل الدخول
1. افتح التطبيق واضغط على "تسجيل الدخول"
2. أدخل اسم المستخدم وكلمة المرور
3. سيتم توجيهك إلى لوحة التحكم

### 3. إدارة القوائم
من لوحة التحكم:
- أضف قوائم جديدة (مثل: قائمة المشروبات)
- حدد نوع العناصر (مثل: مشروب، طبق، خدمة)
- أضف عناصر لكل قائمة مع الأسعار
- عدّل أو احذف القوائم والعناصر

### 4. تخصيص الإعدادات
من تبويب "إعدادات الحساب":
- اختر السمة المفضلة (5 خيارات)
- أضف رابط شعارك
- أضف رابط صورة الخلفية
- احفظ التغييرات

### 5. عرض قائمتك العامة
- رابط قائمتك: `/menu/[username]`
- مثال: `http://localhost:3000/menu/admin`
- شارك هذا الرابط مع عملائك

## البنية التقنية

### الملفات الرئيسية
- `/app/api/admins/` - إدارة المستخدمين
- `/app/api/auth/` - تسجيل الدخول
- `/app/api/lists/` - إدارة القوائم
- `/app/api/menu/` - إدارة العناصر
- `/app/login/` - صفحة تسجيل الدخول
- `/app/admin/` - لوحة التحكم
- `/app/menu/[username]/` - صفحة العرض العامة
- `/lib/db.ts` - قاعدة البيانات والوظائف
- `/data/menu.json` - ملف تخزين البيانات

### قاعدة البيانات
يستخدم التطبيق JSON file-based database مع الهيكل التالي:
```json
{
  "admins": [],
  "lists": [],
  "items": []
}
```

## API Endpoints

### Admins
- `GET /api/admins` - جلب جميع المستخدمين
- `POST /api/admins` - إنشاء مستخدم جديد
- `GET /api/admins/[id]` - جلب مستخدم محدد
- `PUT /api/admins/[id]` - تحديث مستخدم
- `DELETE /api/admins/[id]` - حذف مستخدم

### Auth
- `POST /api/auth` - تسجيل دخول

### Lists
- `GET /api/lists?adminId=xxx` - جلب القوائم (مع تصفية اختيارية)
- `POST /api/lists` - إنشاء قائمة جديدة
- `GET /api/lists/[id]` - جلب قائمة محددة
- `PUT /api/lists/[id]` - تحديث قائمة
- `DELETE /api/lists/[id]` - حذف قائمة

### Menu Items
- `GET /api/menu` - جلب جميع العناصر
- `POST /api/menu` - إنشاء عنصر جديد
- `GET /api/menu/[id]` - جلب عنصر محدد
- `PUT /api/menu/[id]` - تحديث عنصر
- `DELETE /api/menu/[id]` - حذف عنصر

## السمات المتاحة

1. **المحيط (Ocean)**: #0ea5e9, #06b6d4
2. **الغروب (Sunset)**: #f97316, #fb923c
3. **الغابة (Forest)**: #10b981, #34d399
4. **الملكي (Royal)**: #8b5cf6, #a78bfa
5. **الوردي (Rose)**: #ec4899, #f472b6

## نصائح للاستخدام

### للحصول على أفضل النتائج:
- استخدم صور شعار بخلفية شفافة (PNG)
- حجم الشعار الموصى به: 200x200 بكسل
- استخدم صور خلفية عالية الجودة (1920x1080)
- اختر سمة تتناسب مع ألوان علامتك التجارية

### أمثلة على الاستخدام:
- مطعم: قوائم (مقبلات، أطباق رئيسية، حلويات)
- مقهى: قوائم (قهوة، شاي، عصائر)
- صالون: قوائم (قص شعر، صبغة، عناية)

## التطوير المستقبلي
- [ ] نظام الطلبات المباشرة
- [ ] تصدير القائمة كـ PDF
- [ ] دعم اللغات المتعددة
- [ ] نظام التقييمات والتعليقات
- [ ] تحليلات وإحصائيات

## الدعم
للمساعدة والأسئلة، يرجى فتح issue في المستودع.

---
تم التطوير باستخدام Next.js 15 و TypeScript و TailwindCSS
