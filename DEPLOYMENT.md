# دليل رفع المشروع مجاناً

## الطريقة 1: Vercel (الأفضل والأسهل)

### الخطوات:

1. **إنشاء حساب على Vercel**
   - اذهب إلى https://vercel.com
   - سجل دخول باستخدام GitHub

2. **رفع المشروع إلى GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin YOUR_GITHUB_REPO_URL
   git push -u origin main
   ```

3. **ربط المشروع بـ Vercel**
   - اضغط "New Project"
   - اختر المشروع من GitHub
   - اضغط "Deploy"

4. **إضافة المتغيرات البيئية (Environment Variables)**
   - في لوحة تحكم Vercel → Settings → Environment Variables
   - أضف:
     ```
     ADMIN_API_KEY=dev-api-key-2024-menu-app-secure
     SESSION_SECRET=session-secret-2024-menu-app-super-secure-random-string
     ```

5. **انتهى!**
   - سيكون موقعك متاحاً على: `https://your-project.vercel.app`

---

## الطريقة 2: Netlify

### الخطوات:

1. **إنشاء حساب على Netlify**
   - اذهب إلى https://netlify.com

2. **تثبيت Netlify CLI**
   ```bash
   npm install -g netlify-cli
   ```

3. **بناء المشروع**
   ```bash
   npm run build
   ```

4. **رفع المشروع**
   ```bash
   netlify deploy --prod
   ```

5. **إضافة المتغيرات البيئية**
   - في لوحة تحكم Netlify → Site settings → Environment variables

---

## الطريقة 3: Railway

### الخطوات:

1. **إنشاء حساب على Railway**
   - اذهب إلى https://railway.app

2. **إنشاء مشروع جديد**
   - New Project → Deploy from GitHub repo

3. **إضافة المتغيرات البيئية**
   - Variables → Add Variable

---

## ملاحظات مهمة:

### قبل الرفع:

1. **تأكد من ملف `.gitignore`**
   ```
   node_modules/
   .next/
   .env.local
   data/menu.json
   ```

2. **إنشاء ملف `data/menu.json` تلقائياً**
   - المشروع يقوم بإنشاء الملف تلقائياً إذا لم يكن موجوداً

3. **غيّر المفاتيح السرية**
   - غيّر `ADMIN_API_KEY` و `SESSION_SECRET` إلى قيم عشوائية قوية

### بعد الرفع:

1. **اختبر جميع الوظائف**
2. **أنشئ أول حساب admin باستخدام Postman**
3. **شارك رابط موقعك!**

---

## الحد المجاني لكل منصة:

| المنصة | الحد المجاني |
|--------|--------------|
| **Vercel** | ✅ 100GB Bandwidth/شهر، مشاريع غير محدودة |
| **Netlify** | ✅ 100GB Bandwidth/شهر، 300 دقيقة build |
| **Railway** | ✅ $5 رصيد مجاني شهرياً |

---

## الخيار الموصى به:

🎯 **Vercel** هو الأفضل لمشاريع Next.js:
- سهل جداً
- سريع
- تحديث تلقائي عند كل push
- دعم ممتاز لـ Next.js
